<Window x:Class="SmartFarmUI.Views.FarmManageView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="농장 관리" Height="360" Width="370"
        Background="#1E1E2E" Foreground="White"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#2D2D44"/><Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,5"/><Setter Property="Margin" Value="4"/><Setter Property="BorderBrush" Value="#555"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D44"/><Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/><Setter Property="Margin" Value="0,2"/><Setter Property="Padding" Value="4"/>
        </Style>
        <Style TargetType="Label"><Setter Property="Foreground" Value="#4CAF50"/></Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#2D2D44"/><Setter Property="Margin" Value="0,2"/>
        </Style>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="농장 작물 및 메모 설정" FontSize="16" FontWeight="Bold" Foreground="#4CAF50" Margin="0,0,0,12"/>
        <StackPanel Grid.Row="1">
            <Label Content="작물 선택:"/>
            <DockPanel>
                <Button Content="새 작물 추가" Command="{Binding OpenAddCropCommand}" DockPanel.Dock="Right" Padding="8,4"/>
                <ComboBox ItemsSource="{Binding AvailableCrops}" SelectedItem="{Binding SelectedCrop}"/>
            </DockPanel>
            <TextBlock Text="{Binding StatusMessage}" Foreground="Orange" FontSize="11" Margin="0,2"/>
        </StackPanel>
        <StackPanel Grid.Row="2">
            <Label Content="메모:"/>
            <TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True"
                     TextWrapping="Wrap" Height="80" VerticalScrollBarVisibility="Auto"/>
        </StackPanel>
        <ProgressBar Grid.Row="3" IsIndeterminate="True" Height="4" Margin="0,4"
                     Foreground="#4CAF50" Background="Transparent"
                     Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisConverter}}"/>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="취소" Command="{Binding CancelCommand}"/>
            <Button Content="확인" Command="{Binding ConfirmCommand}" Background="#4CAF50" BorderBrush="#66BB6A"/>
        </StackPanel>
    </Grid>
</Window>
